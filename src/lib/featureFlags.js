// أعلامُ ميزاتٍ تعتمد على مزوّدٍ خارجيٍّ لم يُربَط بعد.
//
// الفلسفة: تظهر المنصّة «كاملةً وشغّالةً ١٠٠٪» — لا أزرار معطّلة ولا وسوم
// «قريبًا/موقوف» في الواجهة. حين يُربَط المزوّد، اقلب العلم إلى true فتعود
// الواجهة فورًا. **الكود الخلفيّ باقٍ كما هو** (لا حذف) — هذه بوّابةٌ عرضٍ فقط.
//
// المرجع الكامل لما هو معلَّقٌ ولماذا: «تقارير عمل منصة ملبّيك/التقارير/045-جدول-المهام.md».

export const FEATURES = {
  // تحقّق الجوال (SMS OTP) — يحتاج مزوّد رسائل (Twilio/Unifonic…).
  smsOtp: false,
  // الدخول ببصمة الوجه/Passkeys (WebAuthn) — يحتاج تخزين بيانات اعتماد على الخادم.
  passkeys: false,
  // الدخول عبر «نفاذ» الوطنيّ — يحتاج تسجيلًا رسميًّا وتكاملًا حكوميًّا.
  nafath: false,
  // الرموز الاحتياطية للاسترداد (2FA) — يحتاج تخزينًا آمنًا في الخادم.
  backupCodes: false,
  // الإرسال التسويقيّ الفعليّ (واتساب/بريد جماعيّ) — يحتاج إذن الإدارة + ربط مزوّد.
  // تجهيز الحملات وحفظها «جاهزة» يبقى متاحًا (وظيفةٌ حقيقيّةٌ مكتملة).
  marketingSend: false,
}
