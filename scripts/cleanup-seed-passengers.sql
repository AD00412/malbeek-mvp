-- ============================================================
--  ملبّيك — حذفُ معتمري بذرة اختبار الكشف (seed-manifest-test.mjs)
--  آمنٌ ومحدّدٌ: يحذف فقط الصفوف الموسومة، لا يمسّ أيّ بياناتٍ أخرى.
--  شغّله في Supabase SQL Editor عند انتهاء اختبار الترقيم.
-- ============================================================

-- الموسومون بـ notes (المصدر الأساسيّ للوسم)
delete from public.passengers where notes = 'بذرة-اختبار-كشف';

-- احتياطٌ: أيُّ بقايا بالوسم البصريّ في الاسم
delete from public.passengers where full_name like '[بذرة-اختبار]%';

-- تحقّق (يُفترض ٠)
-- select count(*) from public.passengers where notes = 'بذرة-اختبار-كشف';
